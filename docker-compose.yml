services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: nba-backend
    environment:
      - PORT=4000
      - NODE_ENV=production
    ports:
      - "4000:4000"
    restart: unless-stopped